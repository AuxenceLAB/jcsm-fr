{
  "name": "jcsm.fr",
  "version": "1.0.0",
  "description": "Site public JCSM (jcsm.fr) — seule étape de build : compilation Tailwind CSS vers css/tailwind.css",
  "private": true,
  "license": "ISC",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -o css/tailwind.css --minify && node scripts/build-css-bundle.cjs",
    "watch:css": "tailwindcss -c tailwind.config.js -o css/tailwind.css --watch",
    "check": "bash scripts/check.sh",
    "build:bundle": "node scripts/build-css-bundle.cjs"
  },
  "devDependencies": {
    "lightningcss": "^1.33.0",
    "tailwindcss": "^3.4.19"
  }
}
